$("#message_area").html("<%= j(render 'messages/index', { messages: @messages, over_ten: @over_ten }) %>")
$('.notice_area').html("<%= j(render 'layouts/notice') %>")
$("textarea").val('')

setTimeout(flashNotice, 3000);
function flashNotice(){
  $('.notice').fadeOut();
}
