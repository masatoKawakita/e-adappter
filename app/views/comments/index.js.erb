$("#comments_area").html("<%= j(render 'comments/index', {comments: @comment.advertisement.comments.order(created_at: :desc), advertisement: @comment.advertisement }) %>");
$('.header__flashMessage').append("<%= j(render 'layouts/notice') %>");
$("textarea").val('');
$("#numDiv").html("<%= escape_javascript(render partial: 'advertisements/counter', locals: { advertisement: @advertisement }) %>");


setTimeout(flashNotice, 2000);
function flashNotice(){
  $('.header__flashMessage_type_notice').fadeOut().queue(function() {
    $(this).remove();
  });
}
