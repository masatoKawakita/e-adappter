$('.header__flashMessage').append("<%= j(render 'layouts/alert') %>");

setTimeout(flashAlert, 5000);
function flashAlert(){
  $('.header__flashMessage_type_alert').fadeOut().queue(function() {
    $(this).remove();
  });
};
